export enum MemberRole {
	VIEWER = "viewer",
	MEMBER = "member",
	ADMIN = "admin",
	OWNER = "owner",
}

export enum PlanTier {
	FREE = "free",
	PRO = "pro",
	BUSINESS = "business",
	ENTERPRISE = "enterprise",
}

export enum InvitationStatus {
	PENDING = "pending",
	ACCEPTED = "accepted",
	EXPIRED = "expired",
	REVOKED = "revoked",
}
