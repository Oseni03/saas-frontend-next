export enum MemberRole {
	OWNER = "OWNER",
	ADMIN = "ADMIN",
	MEMBER = "MEMBER",
}

export enum PlanTier {
	FREE = "FREE",
	PRO = "PRO",
	BUSINESS = "BUSINESS",
	ENTERPRISE = "ENTERPRISE",
}

export enum InvitationStatus {
	PENDING = "pending",
	ACCEPTED = "accepted",
	EXPIRED = "expired",
	REVOKED = "revoked",
}
